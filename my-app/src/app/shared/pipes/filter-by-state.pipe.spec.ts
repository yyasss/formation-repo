import { FilterByStatePipe } from './filter-by-state.pipe';

describe('FilterByStatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByStatePipe();
    expect(pipe).toBeTruthy();
  });
});
